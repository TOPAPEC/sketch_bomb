# Шаблон презентации к защите — Sketch Bomb

> Структура слайд-в-слайд по правилам защиты. Реальные цифры уже подставлены из
> отчётов репозитория. `⬜` — то, что нужно вписать/доснять тебе (машина не знает).
> Конвертировать в слайды: скопировать в PowerPoint/Google Slides, либо рендер
> через Marp/Slidev (каждый `---` = новый слайд).

---

## Слайд 1 — Титульный

**Sketch Bomb** — интеллектуальный ассистент для художников: превращение грубых
набросков в чистые реалистичные изображения (ControlNet + диффузия + автоматический отбор).

- **Команда:** Данил Бугриенко (`@TOPAPEC`)
- **Куратор:** Глеб Булыгин (`@jdbelg`)
- **Тема:** 90, уровень **pro**
- **Номер команды:** ⬜ _(вписать из таблицы)_
- GitHub: https://github.com/TOPAPEC/sketch_bomb

---

## Слайд 2 — Постановка задачи

**Задача.** Пользователь рисует грубый скетч прямо на сайте (мышью/пальцем), **без
текстовых подсказок**. На выходе — чистое реалистичное изображение задуманного объекта
с убранным фоном.

**Почему это нетривиально (вывод КТ-2).** Диффузия *без явного промпта* не умеет
«дорисовать» смысл скетча — выходит либо копия наброска, либо шум. Значит, скетч нужно
сначала **распознать**, развернуть в промпт и только потом генерировать. Отсюда весь
пайплайн.

**Цель.** Превратить набросок за 20 секунд в презентабельную картинку без навыков
рисования и без ручного промпт-инжиниринга.

**Кому полезно / какие боли закрываем** ⬜ _(сформулировать — ниже черновик-затравка):_
- _концепт-художники / иллюстраторы: быстрый визуальный превью идеи из наброска;_
- _непрофессионалы: «нарисовал каляку — получил картинку», без Stable-Diffusion-промптов;_
- _геймдев / сторибординг: быстрый набор вариантов объекта на прозрачном фоне (ассеты)._

---

## Слайд 3 — Описание данных

| | QuickDraw | DomainNet (sketch) | Датасет классификатора (30 кл.) |
|---|---|---|---|
| Формат | штрихи (вектор, NDJSON) | растр JPG/PNG | рендер QuickDraw → .npy |
| Классов | 345 | 345 | 30 «трудных» |
| Объём | ~150K+/класс | 2 965 на 10 классов | 4500/класс → 90k/15k/30k |
| Источник | игра Google за 20 сек | скрейп из интернета | QuickDraw simplified |

**Особенности / «грязь» данных:**
- QuickDraw — **шумные лейблы** (напр. «cat» #4 — на деле геймпад), качество гуляет.
- DomainNet — **мусор**: фотографии, тёмные фоны, цветные иллюстрации → фильтруем по
  SigLIP2 (`"a sketch drawing of a {class}"`), отсекаем нижние 50% (ниже медианы).
- Пропусков нет (данные рендерятся), «выбросы» = нечитаемые/мусорные скетчи.
- Датасет классификатора **сбалансирован** by-construction, сплиты непересекающиеся,
  seed фиксирован (утечка проверена: дублей test↔train 1/30000).

_Картинки: `eda_out/`, `eda_30c_out/`, коллажи примеров по классам._

---

## Слайд 4 — Ключевые результаты EDA

_(два ключевых вывода; при желании разбить на два физических слайда)_

### 4.1 Классификация скетча — бутылочное горлышко

Готовый BEiT на 30 трудных классах (предсказывая по всем 345 — отсюда срывы в
визуально-соседние классы вне нашей тридцатки):
- top-1 = **64.8%**, top-3 = 84.8%, top-5 = 89.6%; разброс **12%** (aircraft carrier) … **100%** (hat)
- топ-путаницы: `cookie→potato` (11/16), `broom→rake` (10), `face→smiley` (5)
- «тип-3 — полный бред» (`passport→power outlet`, `spider→sun`) **необратимо ломает генерацию**

→ **нужен свой классификатор**; метрика отбора генерации должна быть **семантической**.

### 4.2 Половина DomainNet — мусор → фильтрация через SigLIP2

Каждую картинку скорим SigLIP2 по промпту `"a sketch drawing of a {class}"` и режем всё
**ниже медианы класса**: **2965 → 1491** картинок (ровно 50%). Что и с какими скорами
надёжно отлетает:

| Что уходит вниз | Типичный SigLIP2 |
|---|---|
| фотографии | −8 … −12 |
| чёрный / тёмный фон | −10 … −17 |
| цветные иллюстрации | −3 … −7 |
| коллажи из нескольких объектов | −2 … −6 |
| «не тот объект» (тигр в `face`, гибискус в `tree`) | ловится |

- Самые грязные классы: `tree` (std 2.87, min −17.95), `bird`, `face` — **без фильтрации
  почти непригодны**, с ней — нормальные скетчи.
- SigLIP2 предпочитает **изолированный объект** (велосипед без райдера) — ровно то, что
  нужно ControlNet как чистый reference.
- Чего НЕ ловит (честно): вотермарки (Shutterstock) и текст «How to Draw», если сам скетч ок.

→ **DomainNet-matching берём только из отфильтрованного набора** — меньше шансов подобрать
коту фотографию леопарда или чёрный квадрат. _(Картинки top-16 vs bottom-16: `eda_out/filtering/`.)_

> Прим.: DomainNet-matching помогает не всегда даже после чистки — плюс для `house`,
> минус для мультяшных `cat` (см. слайд 8.3).

---

## Слайд 5 — Метрики и обоснование

**Генерация (главная метрика): SigLIP2-score** — близость картинки к промпту
`"a high quality illustration of a {label}"`.
- _Почему она:_ для генерации нет ground-truth; нужна автоматическая прокси-оценка
  **семантического соответствия** объекту. SigLIP2 — sigmoid-contrastive, **калибрована
  по нулю**: >0 ≈ «вероятность соответствия выше 50%».
- _Ограничения (честно):_ шумная, не измеряет анатомию/эстетику напрямую → для этого
  добавлен **multi-criteria** вариант (7 осей: artifacts, anatomy, sharpness, composition…).

**Классификатор:** **top-1** (главная) · top-3/top-5 (важны для пайплайна — можно
генерить по нескольким гипотезам) · **macro-F1** (страховка от дисбаланса; здесь ≈ top-1,
т.к. классы сбалансированы).

---

## Слайд 6 — Стартовое решение (baseline)

Пайплайн состоит из двух подзадач — **классификация скетча** и **генерация картинки** —
у каждой своя метрика, поэтому и бейзлайны раздельные.

**6.1 Подзадача «генерация» — метрика SigLIP2-score** (чем выше, тем лучше; 0 — граница)

| Бейзлайн | SigLIP2 | Комментарий |
|---|---|---|
| SD1.5 + ControlNet, сырой скетч, 1 проход | avg **−0.52** | низко и **нестабильно** (зависит от сида); велосипеды/птицы — провал |
| SDXL + ControlNet, сырой скетч, 1 проход | avg **−0.37** | то же; отрицательный скор = объект часто не считывается |

**6.2 Подзадача «классификация скетча» — метрика top-1 accuracy** (на 30 классах)

| Бейзлайн | top-1 | Комментарий |
|---|---|---|
| BEiT (честно: argmax по тем же 30 классам) | **88.9%** | «потолок» готовой модели на нашей задаче |
| тот же BEiT в реальном пайплайне (argmax по 345 классам) | **64.8%** | каждая 3-я ошибка каскадно ломает генерацию |
| ViT linear-probe (frozen ImageNet) | **87.0%** | почти догоняет, но **не превосходит** 88.9% — нужен full finetune |

**Вывод:** стартовое качество **неудовлетворительное по обеим подзадачам**. Генерация даёт
отрицательный SigLIP2 (объект не считывается, нестабильно); готовый классификатор в
пайплайне ошибается каждый 3-й раз (64.8%), а «тип-3 бред» необратимо ломает картинку.
Отсюда план: свой классификатор + отбор лучшего из нескольких генераций + чистка фона.

---

## Слайд 7 — Эксперименты с ML

⬜ _Классического ML (логрег/бустинг/деревья) в проекте нет — задача целиком DL._
_По правилам этот слайд можно опустить либо честно проговорить:_ «простые ML-бейзлайны
неприменимы — вход это изображение/штрихи, единственный осмысленный простой бейзлайн
(linear probe поверх ViT-фичей, 87%) показан как стартовое решение».

---

## Слайд 8 — Эксперименты с DL + лучшая модель

**8.1 Классификатор скетчей (MLflow, RTX 3090):**

| Модель | Init | Режим | test top-1 | top-3 | macro-F1 | train | VRAM |
|---|---|---|---|---|---|---|---|
| **ViT-base/CLIP** | CLIP (OpenAI) | full finetune | **0.9333** | 0.968 | 0.933 | 2166 s | 12.4 GB |
| ViT-base | ImageNet-21k | full finetune | 0.9306 | 0.967 | 0.931 | 2160 s | 12.3 GB |
| ViT-small | ImageNet-21k | full finetune | 0.9260 | 0.967 | 0.926 | 960 s | 9.3 GB |
| ViT-small | ImageNet-21k | linear probe | 0.8706 | 0.949 | 0.871 | 357 s | 1.0 GB |
| baseline BEiT | — | eval-only | 0.6385 / **0.8895**¹ | 0.829 | — | — | — |

_Гиперпараметры:_ AdamW, LR head 1e-3 / backbone 2–3e-5, cosine+warmup, label smoothing
0.1, fp16, аугм. RandomAffine (±12°, shift 8%, scale 0.85–1.1), без hflip.

> ¹ **Два числа BEiT — это две РАЗНЫЕ задачи, не путать:** **0.8895** = argmax только по
> нашим 30 классам — это **честное яблоко-к-яблоку** с нашим ViT (тот же набор классов).
> **0.6385** = argmax по всем 345 классам — это «реальное поведение в пайплайне», но
> **другая, более сложная задача** (345-way против 30-way). Сравнивать 93.3% ViT нужно с
> **88.9%**, а не с 63.8%.

**8.2 Генерация (выбор модели, 80 скетчей, 4 кандидата):**

| Модель | Скорость | SigLIP2 best-of-4 | без отбора |
|---|---|---|---|
| SD 1.5 | 1.81 s | +1.438 | −0.391 |
| SDXL | 13.79 s | +1.192 | −0.592 |
| **SDXL-Lightning** | 2.05 s | **+1.870** | +0.207 |

**8.3 Эволюция пайплайна** (BestOfStudy): baseline → DomainNet-matching → фильтрация →
best-of-N (SigLIP2) → удаление фона. Каждый шаг добавляет качество и стабильность.

> ⭐ **Лучшая конфигурация среди всех:** SDXL-Lightning + DomainNet-matching +
> **best-of-4 SigLIP2** → SigLIP2 **+1.87** при ~2 сек/картинку. Классификатор —
> **ViT-base/CLIP 93.3%**, честный выигрыш **+4.4 п.п.** к BEiT на **тех же 30 классах**
> (88.9%). _(64.8% у BEiT — это 345-классовый режим всего пайплайна, другая задача; как
> меру превосходства модели его использовать нельзя.)_

---

## Слайд 9 — Сервисная часть + ДЕМО

**Веб-сервис (FastAPI + canvas-фронтенд).** Рисуешь скетч → пайплайн стримится по
стадиям (SSE, превью на каждом шаге):

`BEiT-классификация → DomainNet-matching → ControlNet+Диффузия (сырые кандидаты) →`
`Удаление фона (BiRefNet, отдельная стадия) → SigLIP2-отбор лучшего → финальная вырезка`

- BiRefNet на GPU (×33 быстрее rembg); best-of-N; выбор модели (Lightning/SD1.5/SDXL) и
  селектора (SigLIP / SigLIP-multi / Kimi); скачивание PNG; история генераций.
- _Выбор модели удаления фона:_ сравнивали **BiRefNet (GPU)** vs **rembg u2netp (CPU)** vs
  GrabCut/Threshold — BiRefNet даёт чистую маску и ×33 по скорости (картинка-сравнение
  `07_bg_removal_comparison/`; количественные SigLIP2-таблицы по методам — в `experiments.md`).
- Фишка для наглядности: **сырой выход диффузии и обесфоненные кандидаты — две отдельные
  стадии**, видно, как из нескольких чистых вариантов отбирается лучший.

🎥 **Демо-видео 30–40 сек:** ⬜ _вставить прямо в слайд (gif/mp4)._
_Сервис запущен: VS Code → Remote-SSH на инстанс → forward port 8000 → http://localhost:8000._

---

## Слайд 10 — Логирование и мониторинг

- **MLflow** (SQLite-бэкенд прямо в репо, `sketch_clf/mlflow.db`): на каждый прогон
  обучения — гиперпараметры, число параметров, per-epoch `train_loss`/`val_top1/3/5`/
  `macro_f1`, финальный тест, время, пик VRAM + артефакты (чекпойнт, confusion, per-class).
  Просмотр: `mlflow ui --backend-store-uri sqlite:///sketch_clf/mlflow.db`.
- **Сервис:** процесс под **supervisor** (autorestart), логи `/var/log/portal/sketchbomb.log`;
  пошаговая **SSE-трассировка** пайплайна (каждая стадия видна в UI и в потоке).
- ⬜ _Зона роста (можно проговорить честно): метрик сервиса (латентность/ошибки запросов,
  Prometheus/Grafana) пока нет — MLflow покрывает эксперименты обучения, не рантайм сервиса._

---

## Слайд 11 — Разделение ролей / что делалось за год

⬜ _Проект сольный (Данил Бугриенко) + куратор. «Все делали всё» неприменимо — вместо
этого дать **таймлайн по контрольным точкам** (что сделано в каждый период). Черновик:_

| Период / КТ | Что сделано |
|---|---|
| Старт | ⬜ постановка, сбор данных, первые пробы SD1.5/SDXL/Flux |
| КТ-2 | ⬜ вывод: без распознавания не работает; переход на формат QuickDraw |
| EDA | ⬜ анализ QuickDraw/DomainNet, выявление слабости BEiT (64.8%) |
| Классификатор | ⬜ свой ViT/CLIP, MLflow, 93.3%, анализ ошибок/устойчивости |
| Пайплайн | ⬜ DomainNet-matching, best-of-N, фильтрация, BiRefNet |
| Сервис | ⬜ FastAPI + фронтенд, стриминг по стадиям |

---

## Слайд 12 — Итоги

**Сделано:**
- свой **классификатор скетчей** (ViT-base/CLIP) — **93.3%** top-1, честный выигрыш
  **+4.4 п.п.** к BEiT на **тех же 30 классах** (88.9%); в полном 345-классовом пайплайне
  BEiT даёт лишь 64.8% — но это другая, более сложная задача, не мера превосходства.
  MLflow-трекинг, анализ ошибок и устойчивости;
- полный **sketch-to-image пайплайн**: классификация → DomainNet-matching → ControlNet +
  диффузия → best-of-N отбор по SigLIP2 → удаление фона;
- **веб-сервис** с пошаговой визуализацией пайплайна и демонстрацией «выбора» лучшего.

**Результат:** лучшая генерация — **SDXL-Lightning + best-of-4 SigLIP2 = +1.87** (~2 с/шт);
качество поднято от нестабильного отрицательного baseline до стабильно положительного.

**Репозиторий:** https://github.com/TOPAPEC/sketch_bomb

---

### Что осталось доделать тебе (чек-лист `⬜`)
1. Демо-видео 30–40 сек (сервис уже запущен — записать через VS Code port-forward).
2. Бизнес-постановка: кому полезно / какие боли (слайд 2).
3. Номер команды из таблицы (слайд 1).
4. Слайд ролей → таймлайн по КТ (слайд 11).
5. _(опц.)_ Честная оговорка про мониторинг сервиса (слайд 10).
